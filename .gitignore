index.html
style.css
script.js
app.js
*.json
*.txt
pages/*.html
assets/js/*.js
# Ignore all files inside the reports folder
/reports/
# But keep the folder itself (so it exists in repo)
!reports/.gitkeep

# Ignore reports and logs
# Ignore reports and logs