[pytest]
log_cli = true
log_cli_level = INFO
log_file = logs/pytest.log
log_file_level = INFO
pythonpath = .
testpaths = tests
addopts = -ra -q
markers =
    smoke: fast, critical-path checks for CI
    regression: full suite
    ddt: data-driven tests
    yw_t2: Register smoke (YW-T2)
